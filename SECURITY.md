# 보안 및 개인정보 처리

이 애플리케이션은 영수증 처리를 로컬에서 수행하도록 설계되었습니다. 다만 원본 문서와 생성 파일에는 결제·주소·사업자 정보가 포함될 수 있으므로 다음 원칙을 지켜야 합니다.

- 원본 영수증과 생성 Excel을 공개 저장소, 이슈, 로그에 첨부하지 않습니다.
- OCR 임시 파일은 처리가 끝난 뒤 삭제하고, 복구 가능한 공유 폴더를 임시 경로로 사용하지 않습니다.
- PaddleOCR 모델은 공식 배포 경로에서만 설치합니다.
- 배포 전 의존성 취약점과 라이선스를 별도로 검토합니다.
- 문제 보고 시 실제 값을 합성값으로 대체하고 재현 가능한 최소 정보만 제공합니다.

보안 문제는 공개 이슈 대신 저장소 소유자에게 비공개 채널로 보고하세요. 공개 전에는 소유자가 연락처와 응답 정책을 이 문서에 추가해야 합니다.
